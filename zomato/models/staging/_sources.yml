sources:
  - name: raw              # Snowflake schema containing raw tables
    database: zomato     # Replace with your database
    schema: raw
    tables:
      - name: orders
      - name: restaurants
      - name: users
      - name: menu
      - name: order_items
      - name: reviews
      - name: food

  - name: ai
    database: ZOMATO
    schema: AI
    description: "LLM-enriched tables produced by ai/enrich_reviews.py (OpenAI)."
    tables:
      - name: review_enriched
        description: "Per-review sentiment + topic + key issue from the LLM."